export const testMe = (a, b) => {
    return a * b;
};

/**
 * Adds two numbers together...
 * @param {Number} a 
 * @param {Number} b 
 * @returns {Number} a + b
 */
export const addUp = (a, b) => {
    return a + b;
};
